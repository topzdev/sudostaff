const ContactModel = require("./ContactModel");
const DepartmentModel = require("./DepartmentModel");
const DesignationModel = require("./DesignationModel");
const LeaveRequestModel = require("./LeaveRequestModel");
const EmployeeModel = require("./EmployeeModel");
const LeaveStatusModel = require("./LeaveStatusModel");
const LeaveTypeModel = require("./LeaveTypeModel");

module.exports = function () {
  // ContactModel.sync({ force: true });
  // DepartmentModel.sync({ force: true });
  // EmployeeModel.sync({ force: true });
  // DesignationModel.sync({ force: true });
  // LeaveStatusModel.sync({ force: true });
  // LeaveTypeModel.sync({ force: true });
  // LeaveRequestModel.sync({ force: true });
};
